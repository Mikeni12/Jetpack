<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.jetpack">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mvvm.LifecycleActivity"
            android:label="@string/title_activity_mvvm" />
        <activity
            android:name=".room.RoomActivity"
            android:label="@string/title_activity_room" />
        <activity
            android:name=".databinding.basic.BasicBindingActivity"
            android:label="@string/title_activity_basic_binding" />
        <activity
            android:name=".databinding.advanced.AdvancedBindingActivity"
            android:label="@string/title_activity_advanced_binding"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>